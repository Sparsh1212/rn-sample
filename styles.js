import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
    alignItems: "center",
  },
  listHeader: {
    fontSize: 50,
    color: "#fff",
    marginVertical: 20,
  },
  movieTitle: {
    color: "#fff",
    textAlign: "center",
    marginVertical: 10,
  },
});

export default styles;
